# dependencies
node_modules/

# probe / adapter runtime state (machine-specific, never commit)
adapters/**/probe-observed.jsonl
adapters/**/probe-fixture-output.jsonl
adapters/**/*.local.json

# brain relay runtime event logs (machine-local, never commit)
.supernono/

# local tooling / editor state
.claude/
.obsidian/

# logs & OS cruft
*.log
.DS_Store
Thumbs.db
